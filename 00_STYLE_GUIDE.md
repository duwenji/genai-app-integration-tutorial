# 教材スタイルガイド

このガイドは、教材を体系化・簡潔・読みやすく保つための最小ルールです。
[css-tutorial](https://github.com/duwenji/css-tutorial) のスタイルガイドを
生成AI統合パターン教材向けに調整したものです。

## 1. ファイル構成ルール

- カテゴリ先頭は必ず `00-README.md`
- 個別教材は `01-*.md`, `02-*.md` の連番
- サンプル実装はMarkdownファイル内に「実ソースコード」セクションを設けて記載
- コードとプロンプトは別々のコードブロックに分けて記載する

## 2. 見出しの標準順序（個別教材）

個別教材ファイルは、原則として次の順序で記述します。

1. この教材で身につくこと
2. 概要
3. 位置づけ
4. 主要概念・設計判断の解説
5. 実ソースコード（Python / プロンプト例、出典パス明記）
6. 演習課題
7. 理解度チェック

補足:
- 設計判断の比較は表形式で整理する
- 重要なコードは抜粋を含め、全体の概要を理解できる量を目安に
- 「良い例」と「悪い例」を対比して示す
- 外部の確立された設計パターン・記事を引用する場合は出典を明記する
- ai-stock-investing-tutorialの`app/`から実ソースを引用する箇所には、
  引用元のファイルパスを必ず明記する

## 3. 文体ルール

- 1文は短く保つ（目安: 60文字以内）
- 同じ意味のセクションを重複させない
- 抽象語だけで終わらず、1つ以上のコード例・プロンプト例を示す

## 4. コードブロックルール

- 言語指定を必ず付ける（`python`, `bash`, `json`, `text`）
- プロンプト例は ```text コードブロックで囲み、地の文と区別する
- コピペ可能な最小セットに絞る
- 存在しないファイル名やパスを記載しない
- 架空のAPIキーやトークンは `YOUR_API_KEY` のようなプレースホルダーを使う
- コードブロック内は1行90文字以内を目安にする

## 5. 実行結果の示し方

- スクリーンショットの代わりに「実行結果例」セクションでコマンド出力・
  APIレスポンスの抜粋を示す
- 出力例は実際に実行可能なコードと対応させ、矛盾がないようにする

## 6. ナビゲーションルール

- 各ファイル末尾に前後リンクを置く
- カテゴリREADMEには教材リンク一覧を置く
- トップREADMEから全カテゴリへ1クリックで移動可能にする

## 7. レビュー用チェックリスト

- 体系化: 学習目標・完了条件がある
- 簡潔: 重複説明や冗長な全文転載がない
- 読みやすさ: 見出し順と用語が統一されている
- 実用性: 最小実行手順がある
- 整合性: 本文の説明とサンプルコードが矛盾しない
- 出典: 外部記事・既存アプリからの引用に出典が明記されている
- 適法性: ケーススタディが投資助言と誤解されない表現になっている
  （該当箇所は[ai-stock-investing-tutorialの免責事項](../ai-stock-investing-tutorial/DISCLAIMER.md)を参照）
