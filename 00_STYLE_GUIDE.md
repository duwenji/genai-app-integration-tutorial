# 教材スタイルガイド

このガイドは、教材を体系化・簡潔・読みやすく保つための最小ルールです。
[css-tutorial](https://github.com/duwenji/css-tutorial) のスタイルガイドを
生成AI統合パターン教材向けに調整したものです。

## 1. ファイル構成ルール

- カテゴリ先頭は必ず `00-README.md`
- 個別教材は `01-*.md`, `02-*.md` の連番
- サンプル実装はMarkdownファイル内に「実ソースコード」セクションを設けて記載
- コードとプロンプトは別々のコードブロックに分けて記載する

## 2. 見出しの標準順序（個別教材）

個別教材ファイルは、原則として次の順序で記述します。

1. この教材で身につくこと
2. 概要
3. 位置づけ
4. 主要概念・設計判断の解説
5. 実ソースコード（Python / プロンプト例、出典パス明記）
6. 演習課題
7. 理解度チェック

補足:
- 設計判断の比較は表形式で整理する
- 重要なコードは抜粋を含め、全体の概要を理解できる量を目安に
- 「良い例」と「悪い例」を対比して示す
- 外部の確立された設計パターン・記事を引用する場合は出典を明記する
- ai-stock-investing-tutorialの`app/`から実ソースを引用する箇所には、
  引用元のファイルパスを必ず明記する
- `app/`に実装例が無いパターンを解説する場合は、実ソースコード欄の冒頭で
  「本教材のサンプルコードは`app/`に実装例が無いため、汎用サンプルコードで
  解説します。」と明記し、架空のコードであることを読者に分かるようにする

## 3. 文体ルール

- 1文は短く保つ（目安: 60文字以内）
- 同じ意味のセクションを重複させない
- 抽象語だけで終わらず、1つ以上のコード例・プロンプト例を示す

## 4. コードブロックルール

- 言語指定を必ず付ける（`python`, `bash`, `json`, `text`）
- プロンプト例は ```text コードブロックで囲み、地の文と区別する
- コピペ可能な最小セットに絞る
- 存在しないファイル名やパスを記載しない
- 架空のAPIキーやトークンは `YOUR_API_KEY` のようなプレースホルダーを使う
- コードブロック内は1行90文字以内を目安にする

## 5. 実行結果の示し方

- スクリーンショットの代わりに「実行結果例」セクションでコマンド出力・
  APIレスポンスの抜粋を示す
- 出力例は実際に実行可能なコードと対応させ、矛盾がないようにする

## 6. 図表ルール

- アーキテクチャ図・比較図・処理フロー図は**Mermaid形式**で ```mermaid
  コードブロックとして記載する（画像ファイルを作成・添付しない）
- 図の種類は内容に応じて使い分ける
  - 呼び出し方式・コンポーネント間の関係 → `flowchart`
  - 時系列の処理の流れ（LLM呼び出し・キャッシュ判定・確認ステップ等） → `sequenceDiagram`
  - 状態の遷移（キャッシュヒット/ミス等の分岐） → `flowchart` の分岐ノードで表現
- 図は本文の説明を置き換えるものではなく補足とする。図の直前または直後に、
  図だけでは伝わらない設計判断の理由を文章で必ず添える
- ai-stock-investing-tutorialの`app/docs/app-design.md`のMermaid図を
  引用・要約する場合も出典パスを明記する

## 7. ナビゲーションルール

- 各ファイル末尾に前後リンクを置く
- カテゴリREADMEには教材リンク一覧を置く
- トップREADMEから全カテゴリへ1クリックで移動可能にする
- ai-stock-investing-tutorialなど**他リポジトリへのリンク**は相対パスではなく
  GitHub URL（`https://github.com/duwenji/<repo>/blob/master/<path>`）を使う。
  本リポジトリ単体でクローン・閲覧された場合でもリンク切れにならないようにするため

## 8. レビュー用チェックリスト

- 体系化: 学習目標・完了条件がある
- 簡潔: 重複説明や冗長な全文転載がない
- 読みやすさ: 見出し順と用語が統一されている
- 実用性: 最小実行手順がある
- 整合性: 本文の説明とサンプルコードが矛盾しない
- 出典: 外部記事・既存アプリからの引用に出典が明記されている。`app/`に
  実装例が無く汎用サンプルコードを使う場合は、その旨が明記されている
- 図表: 比較・フローを表す箇所がMermaid形式で図示されている
- 適法性: ケーススタディが投資助言と誤解されない表現になっている
  （該当箇所は[ai-stock-investing-tutorialの免責事項](https://github.com/duwenji/ai-stock-investing-tutorial/blob/master/DISCLAIMER.md)を参照）
